interface IProps {}

const AddReview = ({}: IProps) => {
  return (
    <div className="w-full flex flex-col gap-4">
      <h5 className="text-xl text-primaryTitle font-bold">Add Review</h5>
    </div>
  );
};

export default AddReview;
